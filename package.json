{
  "name": "html",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node-sass-chokidar --watch sass --o css --source-map true",
    "build": "node-sass-chokidar sass --o css --source-map true"
  },
  "author": "Mariana Kudrych",
  "license": "ISC",
  "devDependencies": {
    "node-sass-chokidar": "^1.5.0"
  }
}
