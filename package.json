{
  "name": "html",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && node-sass --watch sass --o css --source-map true",
    "build": "node-sass sass --o css --source-map true"
  },
  "author": "Mariana Kudrych",
  "license": "ISC",
  "dependencies": {
    "jquery": "^3.5.1",
    "node-sass": "^4.14.1",
    "normalize.css": "^8.0.1"
  }
}
